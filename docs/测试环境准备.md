## 准备工作

### Injective 环境搭建

按以前的文档来，搭建一个 Injective 的链上环境

### 准备中继者账户

这里新建一个 wcq 的用户，当做喂价的中继者

```bash
echo 12345678 | injectived keys add wcq --home /home/ubuntu/biyachain-core//.injectived/
```

记住这个账户的地址是 inj1f9ja0ed9j3ddl89g8tfw4xfal6xtgqjj80yk9g，私钥是 88358fe1b3e463ca53eaaa522bc4bd75db8a7c62c6339b32aad80ee26d20b685

用创世者账户给 wcq 账户打点 inj 代币，用于后面提交喂价交易消耗 gas 用。

```bash
echo 12345678 | injectived tx bank send genesis inj1f9ja0ed9j3ddl89g8tfw4xfal6xtgqjj80yk9g 10000000000000000000000inj \
   --keyring-backend file \
   --chain-id=injective-1 \
   --node=tcp://localhost:26657 \
   --home=/home/ubuntu/biyachain-core/.injectived \
   --gas auto \
   --gas-adjustment 1.5 \
   --gas-prices 1inj \
   --yes
```

### 准备投票账户

就使用默认创建的三个账户当做投票账户就好（"signer1" "signer2" "signer3"）

使用下面的脚本进行质押成为投票者

```bash
#!/bin/bash
set -e

MY_HOME_DIR=/home/ubuntu/biyachain-core/.injectived
MY_CHAIN_ID=injective-1
KEYPASS=12345678

# 查询验证者
VALIDATOR=$(injectived query staking validators --home $MY_HOME_DIR --chain-id=$MY_CHAIN_ID --output json | jq -r '.validators[0].operator_address')
echo "验证者地址: $VALIDATOR"

# 查询治理参数
POOL=$(injectived query staking pool --home $MY_HOME_DIR --chain-id=$MY_CHAIN_ID --output json)
TOTAL_BONDED=$(echo $POOL | jq -r '.pool.bonded_tokens')
QUORUM=$(injectived query gov params --home $MY_HOME_DIR --chain-id=$MY_CHAIN_ID --output json | jq -r '.params.quorum')

echo "当前总质押: $TOTAL_BONDED"
echo "Quorum 要求: $QUORUM"

# 计算需要的最小投票数
REQUIRED_VOTES=$(echo "scale=0; $TOTAL_BONDED * $QUORUM / 1" | bc)
echo "需要的最小投票数: $REQUIRED_VOTES"

# 查询每个委托人账户的余额和质押（委托人账户需要提前创建好，或者直接用系统内置的）
VALIDATORS=("signer1" "signer2" "signer3")
VALIDATOR1_ADDR="inj10tq6q4p67prfmhmzmdwg7zwx66v0gpfdp6p5l5"
VALIDATOR2_ADDR="inj1kmmz47pr8h46wcyxw8h3k8s85x0ncykqyayvsy"
VALIDATOR3_ADDR="inj1vkjlltxwscft9x9xle53vevxll5jsvuwjsnh80"
VALIDATOR_ADDRS=("$VALIDATOR1_ADDR" "$VALIDATOR2_ADDR" "$VALIDATOR3_ADDR")
TOTAL_VOTING_POWER=0

for i in "${!VALIDATORS[@]}"; do
  val_name="${VALIDATORS[$i]}"
  val_addr="${VALIDATOR_ADDRS[$i]}"

  # 查询余额
  balance=$(injectived query bank balances $val_addr --home $MY_HOME_DIR --chain-id=$MY_CHAIN_ID --output json | jq -r '.balances[] | select(.denom=="inj") | .amount' 2>/dev/null || echo "0")

  # 查询当前质押
  delegation=$(injectived query staking delegations $val_addr --home $MY_HOME_DIR --chain-id=$MY_CHAIN_ID --output json 2>/dev/null | jq -r '.delegation_responses[].balance.amount' 2>/dev/null | awk '{sum+=$1} END {print sum}')
  if [ -z "$delegation" ] || [ "$delegation" == "null" ]; then
    delegation=0
  fi

  echo "$val_name ($val_addr):"
  echo "  余额: $balance"
  echo "  质押: $delegation"

  # 判断是否需要质押
  if [ "$delegation" == "0" ] && [ "$balance" != "0" ]; then
    # 计算质押金额：80% 用于质押，20% 留作 gas 费用
    delegate_amount=$(echo "scale=0; $balance * 0.8 / 1" | bc)

    if [ $(echo "$delegate_amount > 0" | bc) -eq 1 ]; then
      echo "  → 将质押: $delegate_amount"
      # 这里是质押inj代币给验证者，成为委托人，获得投票权（权重和你质押的数量有关系）
      printf '%s\n' "$KEYPASS" | injectived tx staking delegate $VALIDATOR ${delegate_amount}inj \
        --from=$val_name \
        --chain-id=$MY_CHAIN_ID \
        --home $MY_HOME_DIR \
        --keyring-backend file \
        --gas=auto \
        --gas-adjustment=1.5 \
        --gas-prices=500000000inj \
        --yes > /dev/null 2>&1

      sleep 3

      # 更新质押数量
      delegation=$(injectived query staking delegations $val_addr --home $MY_HOME_DIR --chain-id=$MY_CHAIN_ID --output json 2>/dev/null | jq -r '.delegation_responses[].balance.amount' 2>/dev/null | awk '{sum+=$1} END {print sum}')
      if [ -z "$delegation" ] || [ "$delegation" == "null" ]; then
        delegation=0
      fi
      echo "  ✅ 质押完成，当前质押: $delegation"
    fi
  fi

  # 累加投票权重
  TOTAL_VOTING_POWER=$(echo "$TOTAL_VOTING_POWER + $delegation" | bc)
  echo ""
done

echo "=========================================="
echo "总投票权重: $TOTAL_VOTING_POWER"
echo "需要投票数: $REQUIRED_VOTES"

if [ $(echo "$TOTAL_VOTING_POWER >= $REQUIRED_VOTES" | bc) -eq 1 ]; then
  echo "✅ 投票权重充足，满足 quorum 要求"
else
  echo "⚠️  警告：投票权重可能不足以满足 quorum"
fi
```

### 创建代币和市场

现在链上还没有 AAPLX、NVDAX、TSLAX 这些股票代币，我们需要创建这些代币，并创建相对于 USDT 的币对市场。

首先创建代币，并且要 mint 一部分代币，否则代币不会出现在

bank module 的 supply 中，导致创建市场时验证失败。

**创建 AAPLX 代币并铸币**

```bash
echo 12345678 | injectived tx tokenfactory create-denom \
  aaplx \
  "Apple Inc. Stock Token" \
  AAPLX \
  18 \
  --keyring-backend file \
  --home=/home/ubuntu/biyachain-core/.injectived \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes

echo 12345678 | injectived tx tokenfactory mint \
  "1000000000000000000000factory/inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw/aaplx" \
  inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes
```

**创建 NVDAX 代币并铸币**

```bash
echo 12345678 | injectived tx tokenfactory create-denom \
  nvdax \
  "NVIDIA Corporation Stock Token" \
  NVDAX \
  18 \
  --keyring-backend file \
  --home=/home/ubuntu/biyachain-core/.injectived \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes

echo 12345678 | injectived tx tokenfactory mint \
  "1000000000000000000000factory/inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw/nvdax" \
  inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes
```

**创建 TSLAX 代币并铸币**

```bash
echo 12345678 | injectived tx tokenfactory create-denom \
  tslax \
  "Tesla Inc. Stock Token" \
  TSLAX \
  18 \
  --keyring-backend file \
  --home=/home/ubuntu/biyachain-core/.injectived \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes

echo 12345678 | injectived tx tokenfactory mint \
  "1000000000000000000000factory/inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw/tslax" \
  inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes
```

在创建市场之前，首先通过提案修改一下 USDT 代币的 min notional，不然创建市场会失败。先创建一个 json 提案文件，如下

```json
{
  "title": "设置 USDT 最小名义价值",
  "description": "为 USDT 设置最小名义价值，以便创建现货市场",
  "denom_min_notional_proposal": {
    "title": "设置 USDT 最小名义价值",
    "description": "为 USDT 设置最小名义价值，以便创建现货市场",
    "denom_min_notionals": [
      {
        "denom": "peggy0xdAC17F958D2ee523a2206206994597C13D831ec7",
        "min_notional": "0.000000000000000001"
      }
    ]
  }
}
```

然后使用脚本进行提案的提交，并使用投票者们进行投票，如下

```bash
#!/bin/bash
set -e

echo "📝 设置 USDT 最小名义价值"

TX_OUTPUT=$(echo 12345678 | injectived tx exchange batch-exchange-modifications-proposal \
  --proposal=denom_min_notional_proposal.json \
  --deposit=1000000000000000000000inj \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes \
  --output json)

TXHASH=$(echo $TX_OUTPUT | jq -r '.txhash')
echo "  ✅ 提案已提交，TxHash: $TXHASH"

sleep 3

PROPOSAL_ID=$(injectived query tx $TXHASH --home /home/ubuntu/biyachain-core/.injectived --chain-id=injective-1 --output json | \
  jq -r '.events[] | select(.type=="submit_proposal") | .attributes[] | select(.key=="proposal_id") | .value' | tr -d '"')
echo "  📋 提案 ID: $PROPOSAL_ID"

echo "  signer1 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer1 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer2 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer2 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer3 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer3 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes

wait_seconds=30
interval=2
elapsed=0

while true; do
  STATUS=$(injectived query gov proposal "$PROPOSAL_ID" --node=tcp://localhost:26657 -o json | jq -r '.proposal.status')

  if [ "$STATUS" = "PROPOSAL_STATUS_PASSED" ]; then
    echo "✅ 提案通过：$PROPOSAL_ID"
    break
  elif [ "$STATUS" = "PROPOSAL_STATUS_REJECTED" ]; then
    echo "❌ 提案被拒绝：$PROPOSAL_ID"
    break
  else
    if [ $elapsed -ge $wait_seconds ]; then
      echo "⏳ 超时仍为：$STATUS"
      break
    fi
    sleep $interval
    elapsed=$((elapsed + interval))
  fi
done
```

然后创建对应币对的市场，分别是 AAPLX/USDT、NVDAX/USDT、TSLAX/USDT

首先创建 AAPLX/USDT 市场，创建提案并且投票，如下

**创建 AAPLX/USDT 市场**

```bash
#!/bin/bash
set -e

echo "📝 创建AAPLX/USDT市场"

TX_OUTPUT=$(echo 12345678 | injectived tx exchange spot-market-launch \
  AAPLX/USDT \
  factory/inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw/aaplx \
  peggy0xdAC17F958D2ee523a2206206994597C13D831ec7 \
  --min-price-tick-size=0.01 \
  --min-quantity-tick-size=0.001 \
  --min-notional=0.000000000000000001 \
  --maker-fee-rate="0.001" \
  --taker-fee-rate="0.001" \
  --base-decimals=18 \
  --quote-decimals=6 \
  --title="AAPLX/USDT Spot Market" \
  --description="Apple Inc. Stock Token Spot Market" \
  --deposit=1000000000000000000000inj \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes \
  --output json)

TXHASH=$(echo $TX_OUTPUT | jq -r '.txhash')
echo "  ✅ 提案已提交，TxHash: $TXHASH"

sleep 3

PROPOSAL_ID=$(injectived query tx $TXHASH --home /home/ubuntu/biyachain-core/.injectived --chain-id=injective-1 --output json | \
  jq -r '.events[] | select(.type=="submit_proposal") | .attributes[] | select(.key=="proposal_id") | .value' | tr -d '"')
echo "  📋 提案 ID: $PROPOSAL_ID"

echo "  signer1 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer1 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer2 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer2 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer3 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer3 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes

wait_seconds=30
interval=2
elapsed=0

while true; do
  STATUS=$(injectived query gov proposal "$PROPOSAL_ID" --node=tcp://localhost:26657 -o json | jq -r '.proposal.status')

  if [ "$STATUS" = "PROPOSAL_STATUS_PASSED" ]; then
    echo "✅ 提案通过：$PROPOSAL_ID"
    break
  elif [ "$STATUS" = "PROPOSAL_STATUS_REJECTED" ]; then
    echo "❌ 提案被拒绝：$PROPOSAL_ID"
    break
  else
    if [ $elapsed -ge $wait_seconds ]; then
      echo "⏳ 超时仍为：$STATUS"
      break
    fi
    sleep $interval
    elapsed=$((elapsed + interval))
  fi
done
```

**创建 NVDAX/USDT 市场**

```bash
#!/bin/bash
set -e

echo "📝 创建NVDAX/USDT市场"

TX_OUTPUT=$(echo 12345678 | injectived tx exchange spot-market-launch \
  NVDAX/USDT \
  factory/inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw/nvdax \
  peggy0xdAC17F958D2ee523a2206206994597C13D831ec7 \
  --min-price-tick-size=0.01 \
  --min-quantity-tick-size=0.001 \
  --min-notional=0.000000000000000001 \
  --maker-fee-rate="0.001" \
  --taker-fee-rate="0.001" \
  --base-decimals=18 \
  --quote-decimals=6 \
  --title="NVDAX/USDT Spot Market" \
  --description="NVIDIA Corporation Stock Token Spot Market" \
  --deposit=1000000000000000000000inj \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes \
  --output json)

TXHASH=$(echo $TX_OUTPUT | jq -r '.txhash')
echo "  ✅ 提案已提交，TxHash: $TXHASH"

sleep 3

PROPOSAL_ID=$(injectived query tx $TXHASH --home /home/ubuntu/biyachain-core/.injectived --chain-id=injective-1 --output json | \
  jq -r '.events[] | select(.type=="submit_proposal") | .attributes[] | select(.key=="proposal_id") | .value' | tr -d '"')
echo "  📋 提案 ID: $PROPOSAL_ID"

echo "  signer1 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer1 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer2 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer2 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer3 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer3 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes

wait_seconds=30
interval=2
elapsed=0

while true; do
  STATUS=$(injectived query gov proposal "$PROPOSAL_ID" --node=tcp://localhost:26657 -o json | jq -r '.proposal.status')

  if [ "$STATUS" = "PROPOSAL_STATUS_PASSED" ]; then
    echo "✅ 提案通过：$PROPOSAL_ID"
    break
  elif [ "$STATUS" = "PROPOSAL_STATUS_REJECTED" ]; then
    echo "❌ 提案被拒绝：$PROPOSAL_ID"
    break
  else
    if [ $elapsed -ge $wait_seconds ]; then
      echo "⏳ 超时仍为：$STATUS"
      break
    fi
    sleep $interval
    elapsed=$((elapsed + interval))
  fi
done
```

**创建 TSLAX/USDT 市场**

```bash
#!/bin/bash
set -e

echo "📝 创建TSLAX/USDT市场"

TX_OUTPUT=$(echo 12345678 | injectived tx exchange spot-market-launch \
  TSLAX/USDT \
  factory/inj1wvudft7glwprhxnuan49uru85hpax8nmy7hklw/tslax \
  peggy0xdAC17F958D2ee523a2206206994597C13D831ec7 \
  --min-price-tick-size=0.01 \
  --min-quantity-tick-size=0.001 \
  --min-notional=0.000000000000000001 \
  --maker-fee-rate="0.001" \
  --taker-fee-rate="0.001" \
  --base-decimals=18 \
  --quote-decimals=6 \
  --title="TSLAX/USDT Spot Market" \
  --description="Tesla Inc. Stock Token Spot Market" \
  --deposit=1000000000000000000000inj \
  --from genesis \
  --chain-id injective-1 \
  --node tcp://localhost:26657 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas auto \
  --gas-adjustment 1.5 \
  --gas-prices 500000000inj \
  --yes \
  --output json)

TXHASH=$(echo $TX_OUTPUT | jq -r '.txhash')
echo "  ✅ 提案已提交，TxHash: $TXHASH"

sleep 3

PROPOSAL_ID=$(injectived query tx $TXHASH --home /home/ubuntu/biyachain-core/.injectived --chain-id=injective-1 --output json | \
  jq -r '.events[] | select(.type=="submit_proposal") | .attributes[] | select(.key=="proposal_id") | .value' | tr -d '"')
echo "  📋 提案 ID: $PROPOSAL_ID"

echo "  signer1 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer1 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer2 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer2 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer3 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer3 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes

wait_seconds=30
interval=2
elapsed=0

while true; do
  STATUS=$(injectived query gov proposal "$PROPOSAL_ID" --node=tcp://localhost:26657 -o json | jq -r '.proposal.status')

  if [ "$STATUS" = "PROPOSAL_STATUS_PASSED" ]; then
    echo "✅ 提案通过：$PROPOSAL_ID"
    break
  elif [ "$STATUS" = "PROPOSAL_STATUS_REJECTED" ]; then
    echo "❌ 提案被拒绝：$PROPOSAL_ID"
    break
  else
    if [ $elapsed -ge $wait_seconds ]; then
      echo "⏳ 超时仍为：$STATUS"
      break
    fi
    sleep $interval
    elapsed=$((elapsed + interval))
  fi
done
```

### 授权中继者

这一步是用投票者给中继者授权提交各个币对市场价格的权利，由中继者发起，由投票者们进行投票通过。

**授权 AAPLX/USDT 市场**

```bash
#!/bin/bash
set -e

echo "📝 授权AAPLX/USDT市场"

TX_OUTPUT=$(echo 12345678 | injectived tx oracle grant-price-feeder-privilege-proposal \
  AAPLX \
  USDT \
  inj1f9ja0ed9j3ddl89g8tfw4xfal6xtgqjj80yk9g \
  --title="Grant PriceFeed Privilege for AAPLX/USDT" \
  --description="Authorize wcq as PriceFeed Relayer for AAPLX/USDT pair" \
  --deposit="10000000000000000000inj" \
  --from=genesis \
  --chain-id=injective-1 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas=auto \
  --gas-adjustment=1.5 \
  --gas-prices=500000000inj \
  --yes \
  --output json)

TXHASH=$(echo $TX_OUTPUT | jq -r '.txhash')
echo "  ✅ 提案已提交，TxHash: $TXHASH"

sleep 3

PROPOSAL_ID=$(injectived query tx $TXHASH --home /home/ubuntu/biyachain-core/.injectived --chain-id=injective-1 --output json | \
  jq -r '.events[] | select(.type=="submit_proposal") | .attributes[] | select(.key=="proposal_id") | .value' | tr -d '"')
echo "  📋 提案 ID: $PROPOSAL_ID"

echo "  signer1 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer1 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer2 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer2 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer3 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer3 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes

wait_seconds=30
interval=2
elapsed=0

while true; do
  STATUS=$(injectived query gov proposal "$PROPOSAL_ID" --node=tcp://localhost:26657 -o json | jq -r '.proposal.status')

  if [ "$STATUS" = "PROPOSAL_STATUS_PASSED" ]; then
    echo "✅ 提案通过：$PROPOSAL_ID"
    break
  elif [ "$STATUS" = "PROPOSAL_STATUS_REJECTED" ]; then
    echo "❌ 提案被拒绝：$PROPOSAL_ID"
    break
  else
    if [ $elapsed -ge $wait_seconds ]; then
      echo "⏳ 超时仍为：$STATUS"
      break
    fi
    sleep $interval
    elapsed=$((elapsed + interval))
  fi
done
```

**授权 NVDAX/USDT 市场**

```bash
#!/bin/bash
set -e

echo "📝 授权NVDAX/USDT市场"

TX_OUTPUT=$(echo 12345678 | injectived tx oracle grant-price-feeder-privilege-proposal \
  NVDAX \
  USDT \
  inj1f9ja0ed9j3ddl89g8tfw4xfal6xtgqjj80yk9g \
  --title="Grant PriceFeed Privilege for NVDAX/USDT" \
  --description="Authorize wcq as PriceFeed Relayer for NVDAX/USDT pair" \
  --deposit="10000000000000000000inj" \
  --from=genesis \
  --chain-id=injective-1 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas=auto \
  --gas-adjustment=1.5 \
  --gas-prices=500000000inj \
  --yes \
  --output json)

TXHASH=$(echo $TX_OUTPUT | jq -r '.txhash')
echo "  ✅ 提案已提交，TxHash: $TXHASH"

sleep 3

PROPOSAL_ID=$(injectived query tx $TXHASH --home /home/ubuntu/biyachain-core/.injectived --chain-id=injective-1 --output json | \
  jq -r '.events[] | select(.type=="submit_proposal") | .attributes[] | select(.key=="proposal_id") | .value' | tr -d '"')
echo "  📋 提案 ID: $PROPOSAL_ID"

echo "  signer1 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer1 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer2 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer2 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer3 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer3 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes

wait_seconds=30
interval=2
elapsed=0

while true; do
  STATUS=$(injectived query gov proposal "$PROPOSAL_ID" --node=tcp://localhost:26657 -o json | jq -r '.proposal.status')

  if [ "$STATUS" = "PROPOSAL_STATUS_PASSED" ]; then
    echo "✅ 提案通过：$PROPOSAL_ID"
    break
  elif [ "$STATUS" = "PROPOSAL_STATUS_REJECTED" ]; then
    echo "❌ 提案被拒绝：$PROPOSAL_ID"
    break
  else
    if [ $elapsed -ge $wait_seconds ]; then
      echo "⏳ 超时仍为：$STATUS"
      break
    fi
    sleep $interval
    elapsed=$((elapsed + interval))
  fi
done
```

**授权 TSLAX/USDT 市场**

```bash
#!/bin/bash
set -e

echo "📝 授权TSLAX/USDT市场"

TX_OUTPUT=$(echo 12345678 | injectived tx oracle grant-price-feeder-privilege-proposal \
  TSLAX \
  USDT \
  inj1f9ja0ed9j3ddl89g8tfw4xfal6xtgqjj80yk9g \
  --title="Grant PriceFeed Privilege for TSLAX/USDT" \
  --description="Authorize wcq as PriceFeed Relayer for TSLAX/USDT pair" \
  --deposit="10000000000000000000inj" \
  --from=genesis \
  --chain-id=injective-1 \
  --home /home/ubuntu/biyachain-core/.injectived \
  --keyring-backend file \
  --gas=auto \
  --gas-adjustment=1.5 \
  --gas-prices=500000000inj \
  --yes \
  --output json)

TXHASH=$(echo $TX_OUTPUT | jq -r '.txhash')
echo "  ✅ 提案已提交，TxHash: $TXHASH"

sleep 3

PROPOSAL_ID=$(injectived query tx $TXHASH --home /home/ubuntu/biyachain-core/.injectived --chain-id=injective-1 --output json | \
  jq -r '.events[] | select(.type=="submit_proposal") | .attributes[] | select(.key=="proposal_id") | .value' | tr -d '"')
echo "  📋 提案 ID: $PROPOSAL_ID"

echo "  signer1 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer1 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer2 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer2 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes
sleep 1
echo "  signer3 投票..."
echo "12345678" | injectived tx gov vote $PROPOSAL_ID yes --from=signer3 --chain-id=injective-1 --home /home/ubuntu/biyachain-core/.injectived --keyring-backend file --gas=300000 --gas-prices=500000000inj --yes

wait_seconds=30
interval=2
elapsed=0

while true; do
  STATUS=$(injectived query gov proposal "$PROPOSAL_ID" --node=tcp://localhost:26657 -o json | jq -r '.proposal.status')

  if [ "$STATUS" = "PROPOSAL_STATUS_PASSED" ]; then
    echo "✅ 提案通过：$PROPOSAL_ID"
    break
  elif [ "$STATUS" = "PROPOSAL_STATUS_REJECTED" ]; then
    echo "❌ 提案被拒绝：$PROPOSAL_ID"
    break
  else
    if [ $elapsed -ge $wait_seconds ]; then
      echo "⏳ 超时仍为：$STATUS"
      break
    fi
    sleep $interval
    elapsed=$((elapsed + interval))
  fi
done
```

至此，环境都准备的差不多了，剩下的就是通过程序，用中继者往链上喂价了。
